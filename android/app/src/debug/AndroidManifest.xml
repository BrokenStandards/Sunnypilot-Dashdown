<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: the throwaway Spike A0 harness for the single-player multi-camera rewrite.
     Not present in release builds. Launch with:
       adb shell am start -n org.sunnypilot.dashdown/.spike.SpikeMultiCamActivity -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name=".spike.SpikeMultiCamActivity"
            android:exported="true"
            android:label="Spike MultiCam" />
    </application>
</manifest>
